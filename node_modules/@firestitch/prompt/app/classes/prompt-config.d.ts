import { MatDialogConfig } from '@angular/material/dialog';
import { Observable } from 'rxjs';
import { FsValuesFunction } from '../interfaces';
import { IFsPromptConfig } from '../interfaces';
import { PromptType } from '../helpers/enums';
export declare class FsPromptConfig<T> {
    config: IFsPromptConfig;
    promptType: PromptType;
    title: string;
    template: string;
    hint: string;
    label: string;
    class: string;
    commitLabel: string;
    cancelLabel: string;
    commitShow: boolean;
    cancelShow: boolean;
    default: any;
    autofocus: boolean;
    type: PromptType;
    buttons: any[];
    values: Observable<T> | Promise<T> | T[] | FsValuesFunction;
    protected _dialogConfig: MatDialogConfig;
    protected _defaultDialogConfig: {
        width: string;
        height: string;
    };
    constructor(config: IFsPromptConfig, promptType: PromptType);
    get dialogConfig(): MatDialogConfig<any>;
    set dialogConfig(value: MatDialogConfig<any>);
    addDefaultPanelClasses(type: string): void;
    protected applyConfig(config: IFsPromptConfig): void;
    protected applyDialogConfig(config: IFsPromptConfig): void;
}
