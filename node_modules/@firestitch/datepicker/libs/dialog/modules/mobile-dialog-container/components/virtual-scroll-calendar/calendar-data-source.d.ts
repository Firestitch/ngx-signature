import { CollectionViewer, DataSource } from '@angular/cdk/collections';
import { Observable } from 'rxjs';
export declare class FsCalendarDataSource extends DataSource<Date> {
    private _cachedMonths;
    private _fetchedPages;
    private readonly _dataStream;
    private readonly _subscription;
    constructor();
    connect(collectionViewer: CollectionViewer): Observable<Date[]>;
    disconnect(): void;
    private _fetchPage;
    private _prepareMonthsList;
}
