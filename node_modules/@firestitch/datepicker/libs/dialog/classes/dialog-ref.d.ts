import { Observable } from 'rxjs';
import { IPeriod } from '../../common/interfaces/period.interface';
import { FsDatePickerDialogModel } from './dialog-model';
import { FsDatePickerOverlayRef } from './overlay-ref';
import { IDialogFactoryOptions } from '../interfaces/dialog-factory-data.interface';
import { IFsDatePickerDialogComponents } from '../interfaces/dialog-components.interface';
export declare class FsDatePickerDialogRef {
    private _pickerOptions;
    private _overlayRef;
    private _dialogModel;
    private _close$;
    private _value$;
    /**
     * Visual components. Can be changed by summary widget but only if _view allowed to do this.
     */
    private _componentsDefault;
    private _components;
    constructor(_pickerOptions: IDialogFactoryOptions);
    get pickerModel(): FsDatePickerDialogModel;
    get options(): IDialogFactoryOptions;
    get value$(): Observable<Date | IPeriod | null>;
    get pickerOverlayRef(): FsDatePickerOverlayRef;
    get close$(): Observable<void>;
    get components(): IFsDatePickerDialogComponents;
    updateValue(value: any): void;
    close(): void;
    private _init;
    private _initModel;
    private _initValue;
    private _initComponents;
}
